dart:
  - dev
os:
  - linux
  - windows

stages:
  - analyze_and_format:
    - group:
      - dartfmt: sdk
      - dartanalyzer: --fatal-infos --fatal-warnings .
  - unit_test:
    - group:
      - command: pub run --preset travis -x browser
