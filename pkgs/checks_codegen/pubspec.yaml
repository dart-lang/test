name: checks_codegen
version: 0.1.0-wip
description: >-
  A companion to `package:checks` to generate type-specific subject extensions
  to read fields.
repository: https://github.com/dart-lang/test/tree/master/pkgs/checks_codegen
issue_tracker: https://github.com/dart-lang/test/issues?q=is%3Aissue+is%3Aopen+label%3Apackage%3Achecks_codegen

resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  analyzer: ^12.1.0
  build: ^4.0.0
  build_test: ^3.5.0
  checks: ^0.3.1
  code_builder: ^4.11.0
  collection: ^1.19.0
  matcher: ^0.12.19
  package_config: ^2.2.0
  path: ^1.9.0
  source_gen: ^4.2.0

dev_dependencies:
  build_runner: ^2.13.0
  test: ^1.31.0
