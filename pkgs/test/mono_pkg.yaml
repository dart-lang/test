# See https://pub.dev/packages/mono_repo

sdk:
- dev

stages:
- unit_test:
  - command: dart test --preset travis test/runner/compiler_runtime_matrix_test.dart -n "concurrently"
    os:
    - windows
    sdk:
    - pubspec
