language: dart

dart:
  - dev

dart_task:
  - test

matrix:
  include:
  - dart: dev
    dart_task: dartfmt
  - dart: dev
    dart_task:
      dartanalyzer: --fatal-infos --fatal-warnings .

branches:
  only: [master]

cache:
  directories:
    - $HOME/.pub-cache
